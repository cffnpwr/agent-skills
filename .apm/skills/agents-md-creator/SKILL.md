---
name: agents-md-creator
description: リポジトリルートのAGENTS.md（AIエージェント向けプロジェクト指示ファイル）を新規作成または改善する。ユーザーが「AGENTS.mdを作って」「AGENTS.mdを改善して」「エージェント向けの指示ファイルを整備して」のようにAGENTS.mdの作成・改善を明示的に依頼した状況で起動する。AGENTS.md不在を検知しても能動的に提案はしない。リポジトリを解析してコードから読み取れる事実（ビルド・テスト・lintコマンド、ディレクトリ構成、使用言語・フレームワーク、既存規約）で下書きを作り、コードから読めない意図（設計方針・優先順位・禁止事項）だけをユーザーに確認するハイブリッド方式で内容を収集する。推奨骨格に沿いつつ該当しないセクションは出さず、常時適用の原則・規約・禁止事項・コマンドを本体に、特定タスク時のみ必要な詳細手順や具体例を.agents/docsにプレーン参照で分離する段階的開示を行う。既存AGENTS.md・CLAUDE.mdのルーブリック監査による改善提案にも対応する。
---

# AGENTS.md Creator

AGENTS.md（AIエージェント向けプロジェクト指示ファイル）の新規作成・改善を行うスキル。

## 起動条件と対象範囲

- 起動するのは、ユーザーがAGENTS.mdの**作成または改善を明示的に依頼した時のみ**。AGENTS.md不在の検知などによる能動提案はしない。
- 対象は**リポジトリルートのAGENTS.md 1枚のみ**。モノレポのサブディレクトリ別ネストAGENTS.mdは対象外。

## 2つのユースケース

1. **新規作成**: AGENTS.mdが存在しないリポジトリでゼロから生成する。「ワークフロー: 新規作成」へ進む。
2. **改善**: 既存のAGENTS.md（またはCLAUDE.md）を監査して差分提案する。「ワークフロー: 改善」へ進む。

着手時にまずルートの `AGENTS.md` の有無を確認し、どちらのユースケースかを判定する。`AGENTS.md` がなく `CLAUDE.md` がある場合は、ユーザーの依頼内容（新規か改善か）に従う。

## 基本原則（両ユースケース共通）

### 記述言語

生成・改善するAGENTS.mdおよび `.agents/docs` 配下の**記述言語はユーザーの使用言語に合わせる**（固定の英語にしない）。やり取りしている言語をそのまま使う。

### 内容収集はハイブリッド方式

最初から全項目を質問攻めにしない。

1. まずリポジトリを解析し、**コードから読み取れる事実**で下書きを作る（ビルド・テスト・lintコマンド、ディレクトリ構成、使用言語・フレームワーク、既存の規約）。
2. **コードから読み取れない意図だけ**をユーザーに確認する（設計方針、優先順位、禁止事項など）。

解析の具体的な観点・手順は [content-gathering.md](references/content-gathering.md) をLOADして従う。

### 推奨骨格 + 適応

推奨セクション群を骨格としつつ、**該当しないセクションは出さない**。
空セクションや「なし」を残してはならない。
推奨骨格の定義と各セクションの判定基準は [recommended-skeleton.md](references/recommended-skeleton.md) をLOADして従う。

### 段階的開示

- **AGENTS.md本体** = どのタスクでも**常時適用される**原則・規約・禁止事項・コマンド。
- **`.agents/docs/`** = 特定タスク時のみ必要な詳細手順・具体例・状況依存ガイド。

「常に読むべきか」の単一軸で振り分ける。
常時適用なら本体、特定タスク時のみなら `.agents/docs`。
切り出しの具体的な判定基準は [progressive-disclosure.md](references/progressive-disclosure.md) をLOADして従う。

### 参照方式

本体から `.agents/docs` への誘導は、**デフォルトではプレーン参照**で行う。
これは必要時にエージェントが読む段階的開示のためで、`@`インポート構文は自動展開されて段階的開示にならない。

プレーン参照の書き方は、**Markdownリンク形式 `[詳細なビルド手順](.agents/docs/build.md)` を推奨**する（クリック可能でエディタ・IDEから辿れ、リンク切れも見つけやすい）。
素のパス記述（例:「詳細は .agents/docs/build.md を参照」）も許容するが、推奨はMarkdownリンク。

ただし `@`参照は禁止ではない。設計意図として、その内容を**どのタスクでも常時コンテキストに読み込ませたい**場合は `@`参照を使ってよい。
使い分けの基準は次のとおり。

- 必要時のみ読めばよい情報 → プレーン参照（段階的開示）。
- どのタスクでも常にコンテキストに載せたい情報 → `@`参照も可。

なお `@`参照は自動展開されるため、Claude以外のエージェントでは無効になりうる点に留意する。

### エントリポイント（CLAUDE.md）

- AGENTS.mdは**実体ファイル**として生成する。
- CLAUDE.md symlinkは**常時生成しない**。
- 実行中のエージェントが**自身をClaudeと認識している場合のみ**、CLAUDE.md → AGENTS.md のsymlink作成をユーザーに提案し、ユーザーの判断で作成する。
  提案の判定基準と手順は [claude-md-entrypoint.md](references/claude-md-entrypoint.md) をLOADして従う。

## ワークフロー: 新規作成

1. ルートに `AGENTS.md` が無いことを確認する。
2. [content-gathering.md](references/content-gathering.md) をLOADし、リポジトリを解析してコードから読み取れる事実を収集する。
3. [recommended-skeleton.md](references/recommended-skeleton.md) をLOADし、該当するセクションだけで下書きを構成する。コードから読めない意図（設計方針・優先順位・禁止事項）をユーザーに確認する。
4. [progressive-disclosure.md](references/progressive-disclosure.md) をLOADし、常時適用でない詳細を `.agents/docs/` に切り出す。本体からはプレーン参照で誘導する。
5. ルートに `AGENTS.md`、詳細があれば `.agents/docs/*.md` を生成する。記述言語はユーザーの使用言語。
6. [claude-md-entrypoint.md](references/claude-md-entrypoint.md) をLOADし、CLAUDE.md symlink提案の要否を判定する。
7. 検証（参照切れ・`@`参照の確認）を行う。手順は「検証」を参照。

## ワークフロー: 改善

1. 既存の `AGENTS.md`（無ければ `CLAUDE.md`）を読む。
2. [rubric-audit.md](references/rubric-audit.md) をLOADし、ルーブリックに沿って監査する。
   監査観点: 推奨骨格の網羅／段階的開示の遵守／リポジトリ実態とのドリフト（記述が古い・不正確）／プレーン参照の参照切れ／CLAUDE.md symlinkの正否／本体の肥大化。
3. ドリフト検知のため [content-gathering.md](references/content-gathering.md) をLOADしてリポジトリ実態を解析し、既存記述と突き合わせる。
4. 監査結果を**差分提案**としてユーザーに提示する。修正方針のうちコードから読めない意図に関わる部分はユーザーに確認する。
5. 合意した修正を適用する。
   本体肥大化の解消で `.agents/docs` へ切り出す場合は [progressive-disclosure.md](references/progressive-disclosure.md) の基準に従う。
6. 検証（参照切れ・`@`参照の確認）を行う。

## 検証

生成・改善後、以下を確認する。

- 本体内のプレーン参照（`.agents/docs/*.md` へのリンク・記述）の参照先ファイルが実在するか（参照切れがないか）。
- 本体に `@`参照がある場合、それが「常時コンテキストに読み込ませたい」という設計意図に沿った意図的なものか（意図しない混入でないか）。
- 空セクションや「なし」だけのセクションが残っていないか。
- 本体が常時適用の情報だけで構成され、肥大化していないか。

参照切れチェックを反復的・機械的に行いたい場合は [check_references.sh](scripts/check_references.sh) を実行してよい（任意。手動確認でも可）。
